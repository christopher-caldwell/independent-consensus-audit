interface:
  display_name: "Independent Consensus Audit"
  short_description: "Produce traceable evidence-aware audit answers"
  default_prompt: "Use $independent-consensus-audit to preserve my question and sources in a request, invoke the local consensus-audit CLI, and report the retained result and limitations."
